using UnityEngine;

namespace Dialogue.Data
{
    internal sealed class GraphflC
    {
        public static readonly string GA = Application.persistentDataPath + "/graphfl.bp";
        public static readonly string GB = Application.persistentDataPath + "/graph.bp";
    }
}